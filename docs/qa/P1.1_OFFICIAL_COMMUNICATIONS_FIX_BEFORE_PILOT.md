# P1.1 — FIX BEFORE PILOT: Comunicados oficiales

Fecha: 2026-09-03 · Entorno: empresa **QA Testing** · Sin publicar, sin emails, sin workers reales.
Base restaurada al finalizar: 9 anuncios históricos, 3 reacciones, 0 versiones, 0 destinatarios, 0 acuses, 0 avisos, 0 overrides de módulo.

## 1. Archivos modificados

| Archivo | Cambio |
|---|---|
| `src/pages/admin/Announcements.tsx` | Carga de versiones por empresa; sets `officialIds` / `lockedIds`; el lápiz de un comunicado oficial abre `OfficialCommunicationDialog` (flujo de versión nueva) y nunca el editor legacy; badges "Comunicado oficial" y "Contenido bloqueado · requiere versión nueva". |

Sin cambios en `MyAnnouncements.tsx`, `OfficialCommunicationDialog.tsx`, `CommunicationDetailDialog.tsx` ni en el modelo `src/lib/announcements/official-communications.ts`.

## 2. Migraciones aplicadas

1. **`announcement_lock_official_content()` + `trg_announcement_lock_official_content`** (BEFORE UPDATE en `announcements`): si el comunicado tiene alguna versión `published`/`superseded`, se rechaza cualquier cambio en `title`, `body`, `media_urls`, `link_url`, `link_label`, `communication_type`. Metadatos operativos (pin, archivado, prioridad) siguen permitidos.
2. **`announcement_version_assign_number()`**: numeración atómica con `pg_advisory_xact_lock` por comunicado (antes `MAX+1` sin bloqueo).
3. **Grants**: `REVOKE ALL` a `anon` en `announcement_versions`, `announcement_recipients`, `announcement_acknowledgments`. `authenticated` queda solo-lectura en destinatarios y acuses; en versiones conserva insert/update (borrador vía RLS) y pierde `DELETE`. `service_role` con `ALL`.
4. **Índice** `idx_announcement_recipients_employee_state (employee_id, state)` para el feed del portal.
5. **EXECUTE revocado** a `PUBLIC`/`anon`/`authenticated` en las dos funciones-trigger.

Linter: 215 avisos, exactamente el baseline previo (los 2 nuevos por SECURITY DEFINER quedaron cerrados con el revoke). Ninguno nuevo introducido.

## 3. Evidencia — bloqueo frontend

- Sin versiones → lápiz = editor legacy (anuncios informativos históricos siguen editables).
- Con versiones → lápiz abre el diálogo oficial, que llama a `announcement_new_version` y edita un **borrador V2**, nunca la versión publicada.
- Tarjeta muestra "Comunicado oficial" y, si hay versión publicada, "Contenido bloqueado · requiere versión nueva".
- `bunx tsgo --noEmit -p tsconfig.app.json` → sin errores.

## 4. Evidencia — bloqueo backend (transacción revertida en QA Testing)

```
IDEMPOTENCIA: OK (unique_violation en el segundo acuse)
BLOQUEO ANNOUNCEMENTS: OK -> Este comunicado oficial ya fue publicado. Para cambiar el contenido crea una versión nueva y publícala.
BLOQUEO VERSION:       OK -> Esta versión ya fue publicada. Crea una nueva versión para cambiar el contenido.
EDICION NO MATERIAL (pinned): OK
V1 acks=1 | V2 acks=0 | V2 pendientes=1 | V1 body="Texto original V1" | numeración V1=1 V2=2
```

Es decir: V1 publicada → acuse V1 → V2 con cambio material → el acuse sigue exclusivamente en V1, V2 nace pendiente y el contenido histórico de V1 permanece intacto. El bloqueo actúa a nivel de base de datos: no depende de la UI.

## 5. QA móvil real (390 × 844, sesión de cuenta ficticia QA)

- Tarjeta pendiente con "REQUIERE CONFIRMACIÓN", empresa, título, "Importante", `v1`, tiempo relativo.
- Selector Español / English sobre la **misma** versión; el cuerpo cambia de idioma sin crear otro comunicado.
- CTA de acuse: altura **44 px** exactos.
- Tras confirmar en inglés: "Confirmado · 9/3/2026, 7:45:15 PM"; recarga → sigue confirmado, **1 solo acuse**, `language_variant = en`.
- `scrollWidth = clientWidth = 390`: sin overflow horizontal. Sin gráficos. Sin errores de consola propios (solo warnings preexistentes de refs en providers).

## 6. Aislamiento y datos protegidos

- Todo el ejercicio ocurrió en QA Testing; Quality Staff y My Staff no fueron tocadas.
- Cero cambios en auth/PIN, payroll, `time_entries`, turnos, asignaciones, pagos, documentos, `pay_statements`, memberships y tenants.
- Cero emails enviados; cero comunicaciones a personas reales.

## 7. Riesgos restantes (no bloqueantes)

1. El encabezado del feed del portal muestra "Announcements" en inglés cuando el idioma es español (etiqueta de navegación, no del comunicado).
2. Borrar un comunicado con evidencia sigue exigiendo desactivar triggers a mano: falta una acción administrativa de archivado en la UI.
3. `my_announcements` no está en los módulos habilitados por defecto: cada empresa debe habilitarlo antes del piloto.
4. `authenticated` conserva insert/update sobre `announcement_versions` (necesario para borradores; contenido publicado ya es inmutable por trigger).

## Veredicto

🟢 READY FOR CONTROLLED REAL PILOT
