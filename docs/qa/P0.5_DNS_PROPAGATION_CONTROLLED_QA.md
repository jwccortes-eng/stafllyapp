# P0.5 — Verificación de propagación DNS + QA controlado de correo

Fecha: 2026-09-04 (UTC) · Última verificación: 20:56 UTC
Modo: **solo lectura**. Cero escrituras, cero correos enviados, cero personas reales tocadas.

---

## A. Estado DNS

**Lado IONOS (registrador): CORRECTO.** La errata de P0.4 (`s3.lovable.cloud`) ya no existe.

Consulta directa al autoritativo del padre (`ns1119.ui-dns.biz`):

```
notify.staflyapps.com. 3600 IN NS ns3.lovable.cloud.
notify.staflyapps.com. 3600 IN NS ns4.lovable.cloud.
```

**Lado Lovable (zona delegada): TODAVÍA NO ACTIVA.** Consulta directa a los dos servidores de destino, repetida durante ~4 minutos (10 sondeos, 20:52–20:56 UTC):

| Servidor | IP | Respuesta a `SOA notify.staflyapps.com` |
|---|---|---|
| ns3.lovable.cloud | 162.159.8.208 | `REFUSED` (10/10 sondeos) |
| ns4.lovable.cloud | 173.245.58.40 | `REFUSED` |

Resolvers públicos: `SERVFAIL` · `EDE(22): No Reachable Authority at delegation notify.staflyapps.com`.

## Registros administrados por Lovable

| Registro | Esperado | Observado |
|---|---|---|
| SPF (TXT `notify`) | `v=spf1 include:mailgun.org ~all` | **ausente** |
| MX (`notify`) | `10 mxa.eu.mailgun.org`, `10 mxb.eu.mailgun.org` | **ausente** |
| DMARC (TXT `_dmarc.notify`) | `v=DMARC1; p=none; …` | **ausente** |
| DKIM | emitido por el proveedor | **ausente** |

Los cuatro están ausentes por la misma causa raíz: la zona no responde. No corresponde crearlos en IONOS.

---

## B. Estado del dominio en el proveedor de correo

- Dominio: `notify.staflyapps.com` — **Verified** en el registro del proveedor.
- **Ruta de envío del proyecto: `Setting up — Confirming email delivery is ready`.**

**Cambio positivo respecto al chequeo anterior:** antes decía `Timed out waiting for email delivery path verification`; el *Retry setup* que ejecutaste **sí surtió efecto** y el aprovisionamiento está corriendo ahora. Lo que falta es que termine y que la zona empiece a responder.

Condición del PASO 2 (VERIFIED **y** READY para envío): **no cumplida**. STOP aplicado.

---

## C. QA ES (`qa-es@staflyapps.com`) — NO EJECUTADO

## D. QA EN (`qa-en@staflyapps.com`) — NO EJECUTADO

Enviar ahora repetiría el rechazo `DOMAIN_UNVERIFIED` de P0.2/P0.3 y ensuciaría el registro de envíos sin aportar evidencia.

---

## E. Verdad de estados de entrega

Modelo de P0.3 intacto y sin tocar:

- `accepted` = el API aceptó la solicitud. **Nunca** se muestra como enviado.
- `sent` solo se fija con evento real de despacho del proveedor.
- `delivered` no se deriva jamás de un envío: la plataforma no emite acuse de entrega ni de apertura. No se inventará.
- `rejected` / `bounced` / `suppressed` son terminales y mandan sobre cualquier estado de progreso.
- `reconcile-email-delivery` avanza de forma monótona (`canAdvance`) e idempotente.
- Los 3 intentos QA del 2026-09-04 siguen auditables como `rejected / DOMAIN_UNVERIFIED`.

Sin envíos nuevos, nada que reconciliar en este turno.

## F. CTA / enlaces

No verificable todavía: requiere correo real en buzón.

## G. Branding e idioma

Revisión estática, sin cambios: `Stafly <noreply@notify.staflyapps.com>` sin contexto de compañía; `"<Compañía> — powered by Stafly"` con contexto. No existe "Staflycore" como nombre visible. Plantillas bilingües ES/EN. Validación en buzón: pendiente.

## H. Matriz de supresión

| Origen | Alcance | Marketing | Operativo | Acceso/activación | Seguridad/recuperación |
|---|---|---|---|---|---|
| unsubscribe | `marketing` | Bloquea | Pasa | **Pasa** | **Pasa** |
| complaint | `non_essential` | Bloquea | Bloquea | **Pasa** | **Pasa** |
| bounce / provider / manual | `all` | Bloquea | Bloquea | Bloquea | Bloquea |

Una baja de marketing nunca inutiliza activación de cuenta ni recuperación de contraseña.

## I. Idempotencia

Sin ejercitar en vivo. Garantías vigentes: clave de idempotencia por evento en cada envío, dedupe por `event_id` en el receptor de eventos, avance monótono de estados.

---

## J. Componente pendiente (único)

**Activación de la zona `notify.staflyapps.com` en los servidores de nombres de Lovable.** El aprovisionamiento está en curso (`Setting up`). No requiere ninguna acción tuya: ni DNS, ni IONOS, ni registros. Solo esperar a que `ns3`/`ns4` dejen de responder `REFUSED` y publiquen SPF/DKIM/MX/DMARC.

Siguiente paso cuando eso ocurra: 1 correo a `qa-es@staflyapps.com` y 1 a `qa-en@staflyapps.com`. Comunicados Oficiales sigue bloqueado hasta cerrar el QA en verde.

## K. Escrituras realizadas

**Ninguna.** Turno exclusivamente de lectura: consultas DNS públicas y autoritativas y lectura del estado del dominio. No se enviaron correos, no se insertaron registros, no se tocó DNS, auth, payroll, time_entries, shift_assignments, scheduled_shifts, pay_statements, movements, period_base_pay, documents, tenants, RLS, PIN, pagos, bookings ni chat. Comunicados Oficiales intacto y sin publicar.

---

🟡 WAITING FOR DNS / PROVIDER
