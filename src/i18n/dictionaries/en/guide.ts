// English (guide / editorial mode). Empty in v1 — falls back to app dictionary.
// Use this when long-form, instructional or "book-like" copy is needed.
const en_guide: Record<string, string> = {};
export default en_guide;
